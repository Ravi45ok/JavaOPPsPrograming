import java.util.*;

public class Empty_Collections {

    public static void main(String[] args) {
        // ! List<T> emptyList()
        // !Set<T> emptySet()
        // !Map<K,V> emptyMap()

        // returns immutable EMPTY collection
        // same shared instance returned every time — no new object
        // useful as default return value — avoids null returns

        // Example

    }
}
